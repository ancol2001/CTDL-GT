#include<stdio.h>
#include<string.h>
#define MAX 1005

void Try(int i, )
int main(){
	int t=1;
//	scanf("%d",&t);
	getchar();
	char str[MAX];
	while(t--){
		char c;
		scanf("%c",&c);
		for(char i=c; i<)
	}
	return 0;
}
